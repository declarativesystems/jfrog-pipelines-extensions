configuration:
  # artifactory used to resolve and publish build artifacts
  sourceArtifactory:
    type: String
    validate:
      notEmpty: true
  # remove container state to speed up subsequent steps
  clean:
    type: Boolean
    default: false
