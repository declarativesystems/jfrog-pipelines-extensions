configuration:
  # aws key resource to use
  awsKey:
    type: String
    validate:
      notEmpty: true

  awsRegion:
    type: String
    validate:
      notEmpty: true

  awsAccountId:
    type: String
    validate:
      notEmpty: true

  commands:
    type: String[]
    validate:
      notEmpty: true

