configuration:
  # artifactory used to resolve artifacts
  sourceArtifactory:
    type: String
    validate:
      notEmpty: true

  # artifactory path to upload to
  path:
    type: String
    validate:
      notEmpty: true

  # Filename to upload
  source:
    type: String
