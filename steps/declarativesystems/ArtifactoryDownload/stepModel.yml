configuration:
  # artifactory used to resolve artifacts
  sourceArtifactory:
    type: String
    validate:
      notEmpty: true

  # path inside artifactory to download
  path:
    type: String
    validate:
      notEmpty: true

  # Filename for downloaded file
  target:
    type: String
